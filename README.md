# implementing_vector
to test 
## intstall gtest
``` apt install libgtest-dev ```
```bash
g++ -o test_program  myvectorTest.cpp -l gtest -l gtest_main  && ./test_program
```
cmake -B build/
cmake --build build/